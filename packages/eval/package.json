{
  "name": "@dispatch-ai/eval",
  "version": "0.0.1",
  "description": "Eval harness for the dispatch-ai detector. Snapshot-based determinism.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "eval:snapshots": "bun run src/run-snapshots.ts",
    "eval:live": "bun run src/run-live.ts"
  },
  "dependencies": {
    "@dispatch-ai/detector": "workspace:*",
    "@dispatch-ai/shared": "workspace:*"
  }
}
