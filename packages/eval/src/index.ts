// Week 1 work (D9+D10): fixture loader (30 known-good + 30 known-bad Python diffs),
// snapshot comparator (compares fresh runs to committed JSON in snapshots/),
// live runner (regenerates snapshots when rubric or model changes).
export {};
